class userAddress{
  String placeFormattedAdress;
  String placeName;
  String placeId;
  double latitude;
  double longitude;

  userAddress({this.placeFormattedAdress, this.placeName, this.placeId, this.latitude, this.longitude});
}